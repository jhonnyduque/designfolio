/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'jhonnyduque.com',
      'images.unsplash.com', // ← agrega esto
    ],
  },
};

module.exports = nextConfig;

